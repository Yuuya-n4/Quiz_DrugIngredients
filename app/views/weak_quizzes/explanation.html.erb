<div class="container mx-auto p-6">
  <div class="bg-white rounded-lg shadow-md p-6 max-w-md mx-auto">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">クイズ解説</h2>

    <p class="text-lg font-bold text-gray-700 mb-2">問題 <%= @current_quiz_number %> / <%= @total_quizzes %></p>

    <div class="bg-blue-100 p-4 rounded-lg mb-4">
      <p class="text-xl font-bold text-gray-700">問題:</p>
      <p class="text-lg text-gray-700"><%= @quiz.question %></p>
    </div>

    <div class="bg-blue-100 p-4 rounded-lg mb-4">
      <p class="text-xl font-bold text-gray-700">正解:</p>
      <p class="text-lg text-gray-700"><%= @quiz.correct_choice.text %></p>
    </div>

    <div class="bg-blue-100 p-4 rounded-lg mb-4">
      <p class="text-xl font-bold text-gray-700">解説:</p>
      <p class="text-lg text-gray-700"><%= @quiz.explanation %></p>
    </div>

    <% if @next_quiz %>
      <%= button_to '次のクイズへ', weak_quiz_path(@next_quiz), method: :get, class: "w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out block text-center" %>
    <% else %>
      <%= button_to 'スコアを見る', score_weak_quizzes_path, method: :get, class: "w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out block text-center" %>
    <% end %>
  </div>
</div>